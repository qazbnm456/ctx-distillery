"""ctx-distillery: an RLM-driven distillation planner for AI coding-agent memory stores.

This package is a downstream consumer of `rlm-kit` (https://github.com/qazbnm456/rlm-kit).
See `docs/DESIGN.md` for the full design and `CLAUDE.md` for the hard invariants this
project must never break (judgement-only, read-only tools, no direct file mutation).
"""

__version__ = "0.1.0"
