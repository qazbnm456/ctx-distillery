# ctx_distillery (package guide — placeholder)

This file will become "the guide" for the `ctx_distillery` package — a walkthrough of the
`DistillSession` task, its tools, the harness-adapter seam, and the promotion/assemble-on-read
convention — once the design in `docs/DESIGN.md` is actually implemented.

For now, read `docs/DESIGN.md` for the full design this scaffold is built against, and the
top-level `README.md` / `CLAUDE.md` for the current (early-scaffold) state of the project.
